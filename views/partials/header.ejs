<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.2.0/fonts/remixicon.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
   <!-- <link rel="stylesheet" href="/stylesheets/profilestyle.css"> -->
  </head>
  <body>


    <nav id="navbar" class="navbar navbar-expand-lg bg-body-tertiary">
      <a class="nav-link" href="/">Home</a>
      <a class="nav-link" href="/feed">All Users</a>
      <a class="nav-link" href="/postss">All Posts</a>
      
      <div id="search">
      
        <input type="text" id="searchUser"  placeholder="Type here!!!">
        <div id="searchedUsers">
        </div>
 
    </div>
    </nav>


  <style>
  #navbar{
  height: 8vh;
  width: 100%;
  background-color: #dddd;
}
#navbar a{
  text-decoration: none;
  font-size: 20px;
  margin-left: 40px;
 text-align: center;
}
#navbar input{
  margin-left: 200px;
  padding: 8px 50px;
  border: 2px solid royalblue;
  border-radius:10px;
  font-size: 20px;
}
#search{
  position: absolute;
  height: 100%;
  width: 18.5%;
  left: 50%;
}
#search input{
   padding: 8px 50px;
  border: 2px solid royalblue;
  border-radius:10px;
  margin-top: 10px
}
#searchUser{
  padding: 8px 50px;
  border: 2px solid royalblue;
  border-radius:10px;
  margin-top: 10px;
}
#resultent{
  max-height: 150px;
 width: 110%;
  background-color: red;
  overflow: auto;
}
.users{
   height: 40%;
   width: 100%;
   margin-left: 220px;
   border: 2px solid rgba(0, 0, 0, 0.477);
   background-color: whitesmoke;
}


  </style>
  <script>    
     document.querySelector("#searchUser").addEventListener('input', function(dets){
       var result = dets.target.value;
        axios.post('/searchUser', {result})
        .then(function(resultent){
             console.log(resultent.data)
            var clutter = ""
            resultent.data.users.forEach(element => {
                // clutter += `<h1>${element.username}</h1>`
                clutter += `<div class="users"><h5>${element.username}</h5></div>`
            });
     document.querySelector("#searchedUsers").innerHTML=clutter;
        }).catch(err=>{
        console.log(err)
    })
    })

// document.querySelector("#searchUser").addEventListener('input', function(dets){
//      console.log (hit); })

    // document.querySelector("#searchUser").addEventListener('input', function(dets){
    //     var result= dets.target.value
    //     axios.post('/searchUser', {result})
    //     .then(function(resultent){
    //         console.log(resultent.data)
    //     })
    // })
 </script>
  </body>
  </html>